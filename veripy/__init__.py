"""VeriPy verifies annotated Python components.

Use ``from veripy import api`` for the stable embedding API. The CLI is
``veripy`` or ``python -m veripy``. Other package modules are internal and
may be reorganized. Payloads are versioned in docs/AGENT-INTERFACE.md.
"""
